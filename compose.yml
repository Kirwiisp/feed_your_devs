services:
  postgres:
    container_name: postgreSQL
    image: postgres
    restart: unless-stopped
    env_file: ".env"
    ports:
      - "5432:5432"
    volumes:
      - ./db:/var/lib/postgresql/data
    networks:
      - backend

  bot:
    container_name: FeedBot
    image: alpine:3.21.3
    restart: unless-stopped
    env_file: ".env"
    volumes:
      - ./app:/rel
    command:
      ""
    networks:
      - backend

networks:
  backend:
    driver: bridge
